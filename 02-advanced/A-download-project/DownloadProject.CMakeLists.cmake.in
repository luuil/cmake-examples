# Distributed under the OSI-approved MIT License.  See accompanying
# file LICENSE or https://github.com/Crascit/DownloadProject for details.

cmake_minimum_required(VERSION 2.8.2)

project(${DP_ARGS_PROJ}-download NONE)

include(ExternalProject)
ExternalProject_Add(${DP_ARGS_PROJ}-download
                    ${DP_ARGS_UNPARSED_ARGUMENTS}
                    SOURCE_DIR          "${DP_ARGS_SOURCE_DIR}"
                    BINARY_DIR          "${DP_ARGS_BINARY_DIR}"
                    CONFIGURE_COMMAND   ""
                    BUILD_COMMAND       ""
                    INSTALL_COMMAND     ""
                    TEST_COMMAND        ""
)
